name: Build docker Image

on: 
    push

jobs: 
    build:
        name: Build docker Image
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
            - name: Build Docker Image
              run: docker build -t holbertonschool-continuous_intgrations:latest .